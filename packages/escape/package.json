{
  "private": true,
  "name": "@ftson/escape",
  "version": "0.0.1",
  "description": "String escape library for `ftson`",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/ftson.git",
    "directory": "packages/escape"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && cpy README.md dist && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "typescript": "^5.8.3"
  }
}
